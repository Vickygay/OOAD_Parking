# OOAD_Parking
To run:
javac Main.java views\*.java controllers\*.java 

Then run in the main java file:
cd "d:\Y2S1\OOAD\FinalAssignment\" && javac main.java && java main

(now haven't put anything for models file so just compile views and contollers file)
